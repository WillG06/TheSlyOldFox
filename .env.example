# ── Email (Resend — recommended) ──────────────────────────────
# Sign up at https://resend.com and create an API key
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxx
FROM_EMAIL=website@theslyoldfox.co.uk
TO_EMAIL=manager@theslyoldfox.co.uk

# ── Email (Nodemailer / SMTP alternative) ──────────────────────
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your@gmail.com
# SMTP_PASS=your-app-password

# ── Database (Prisma + Supabase, if persisting bookings) ───────
# DATABASE_URL=postgresql://user:password@host:5432/slyoldfox

# ── Analytics ─────────────────────────────────────────────────
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX
